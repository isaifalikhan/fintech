import { OrganizationDashboard } from '../OrganizationDashboardModern';

/** Sidebar **Dashboard** (`dashboard`) — `OrganizationDashboard` / ORG-P02. */
export function FinanceOSView() {
  return <OrganizationDashboard />;
}